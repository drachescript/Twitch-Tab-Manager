# Archive Folder

This folder is for older files that were replaced, split up, or kept only as backup/reference.

These files are **not part of the active extension code anymore**

## Purpose

The archive exists so older versions of files are still available for:

- reference
- rollback if needed
- checking older logic during debugging

## Current archived files

### Removed/replaced in `1.0.8.8`

- `background_old_1.0.8.8.js`
  - Old monolithic `background.js` before it was split into the `/bg/` folder modules

- `options_old_1.0.8.8.js`
  - Old monolithic `options.js` before it was split into the `/options/` folder modules

### Older archived docs / backups

- `README_old_1.0.8.2.md`
  - Older README backup from before later updates  

